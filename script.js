// app.js

const fillBtn = document.getElementById('fillBtn');

// Botão não faz nada, só dá um feedback visual no console
fillBtn.addEventListener('click', () => {
  console.log("[The Voider] fill the fog clicado (inativo).");
});
